# Core FastAPI dependencies (production-only, no Rust compilation needed)
fastapi==0.109.0
uvicorn[standard]==0.27.0
httpx==0.26.0
pydantic==2.6.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Note: For local development with full langchain features, install:
# pip install langgraph langchain langchain-core langchain-openai
# These are excluded from production to avoid Rust compilation on Render
